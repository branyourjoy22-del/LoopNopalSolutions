# .claude/

Antes `.openai/`. Solo contiene `hosting.json`, metadata de despliegue del
sitio estático (`project_id`, referencias a bindings de datos/almacenamiento).
No participa en la lógica de ninguna simulación ni se carga desde `index.html`.

Se renombró junto con la incorporación de la simulación 04 (tiempo real).
